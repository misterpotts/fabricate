{
  "name": "fabricate",
  "version": "0.7.0",
  "description": "A system-agnostic, flexible crafting module for FoundryVT",
  "main": "index.js",
  "scripts": {
    "releaseLocal": "npm run test && npm run buildDev && node scripts/releaseLocal.js",
    "build": "webpack build --mode=production --devtool=source-map && npm run compileLess && npm run buildDist",
    "buildDev": "webpack build --mode=development --devtool=eval-source-map && npm run compileLess && npm run buildDist",
    "compileLess": "lessc src/styles/module.less dist/styles/module.css",
    "test": "jest",
    "coverageTest": "jest --collect-coverage --collect-coverage-from='./src/**/*.ts'",
    "buildDist": "mkdir -p dist && npm run copyLanguages && npm run copyCompendia &&  npm run copyReadme && npm run copyManifest &&  npm run copyTemplates &&  npm run copyAssets",
    "copyLanguages": "mkdir -p dist/lang && cp -r src/lang/*.json dist/lang/",
    "copyCompendia": "mkdir -p dist/packs && cp -r src/packs/* dist/packs/",
    "copyReadme": "cp README.md dist/",
    "copyManifest": "cp src/module.json dist/",
    "copyTemplates": "mkdir -p dist/templates/partials && cp -r src/templates/*.hbs dist/templates/ && cp -r src/templates/partials/*.hbs dist/templates/partials/",
    "copyAssets": "mkdir -p dist/assets && cp -r src/assets dist/"
  },
  "author": "Matthew Potts",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.18.10",
    "@babel/preset-env": "^7.18.10",
    "@league-of-foundry-developers/foundry-vtt-dnd5e-types": "^1.5.2-2",
    "@league-of-foundry-developers/foundry-vtt-types": "^9.269.0",
    "@types/chai": "^4.3.3",
    "@types/jest": "^28.1.6",
    "@types/mocha": "^9.1.1",
    "@types/sinon": "^10.0.13",
    "babel-jest": "^28.1.3",
    "chai": "^4.3.6",
    "fancy-log": "^2.0.0",
    "fs-extra": "^10.1.0",
    "gulp": "^4.0.2",
    "gulp-jest": "^4.0.4",
    "gulp-less": "^5.0.0",
    "gulp-mocha": "^8.0.0",
    "gulp-typescript": "^6.0.0-alpha.1",
    "jest": "^28.1.3",
    "mocha": "^10.0.0",
    "nyc": "^15.1.0",
    "sinon": "^14.0.0",
    "ts-jest": "^28.0.7",
    "ts-loader": "^9.3.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4",
    "webpack-cli": "^5.0.0",
    "webpack-stream": "^7.0.0"
  },
  "dependencies": {
    "@types/node": "^18.7.3"
  }
}
